# Staging Environment Configuration
# Production-like environment for final testing

# Database (use staging database)
DATABASE_URL=postgres://staging-user:staging-pass@staging-host:5432/todo_staging

# Security (staging keys - rotate regularly)
JWT_SECRET=staging-jwt-secret-key-minimum-32-characters-long-change-regularly
HASHING_SECRET_KEY=staging-hashing-secret-minimum-16-characters

# Staging settings
RUST_LOG=info
RUST_BACKTRACE=0
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# CORS for staging frontend
FRONTEND_URL=https://staging-frontend.your-domain.com

# Environment indicator
RUST_ENV=staging

# Performance settings (staging)
MAX_CONNECTIONS=25
CONNECTION_TIMEOUT=30
IDLE_TIMEOUT=300